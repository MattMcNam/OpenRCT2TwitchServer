pool:
  vmImage: 'Ubuntu-16.04'

variables:
  imageName: 'mattmcnam/openrct2-twitch-integration:$(build.sourceVersion)'

steps:
- script: docker build --pull -f Dockerfile -t $(imageName) .
  displayName: 'docker build'
